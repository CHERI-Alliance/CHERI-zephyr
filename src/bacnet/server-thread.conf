# Extra config to enable the BACnet server thread (CONFIG_BACNETSTACK_BACNET_SERVER)
# so that server.c (which defines server_thread via k_thread_create + SYS_INIT) is
# actually compiled and enters the compilation database the analyzer reads.
CONFIG_BACNETSTACK_BACNET_SERVER=y